# SandBase Email Validator API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `strale_email_validate` | See `sandbase_describe_tool` for details. |
| `strale_email_deliverability_check` | See `sandbase_describe_tool` for details. |
| `strale_email_reputation_score` | See `sandbase_describe_tool` for details. |
| `strale_mx_lookup` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
