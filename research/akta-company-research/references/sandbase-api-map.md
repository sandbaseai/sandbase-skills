# SandBase Akta Company Research API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `akta_company_enrichment` | See `sandbase_describe_tool` for details. |
| `akta_company_employee_reviews` | See `sandbase_describe_tool` for details. |
| `akta_company_product_reviews` | See `sandbase_describe_tool` for details. |
| `akta_company_search` | See `sandbase_describe_tool` for details. |
| `akta_industry_search` | See `sandbase_describe_tool` for details. |
| `akta_news` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
