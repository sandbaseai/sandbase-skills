# SandBase Community Research API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `reddit_app_subreddit_info` | See `sandbase_describe_tool` for details. |
| `reddit_app_subreddit_feed` | See `sandbase_describe_tool` for details. |
| `reddit_app_community_highlights` | See `sandbase_describe_tool` for details. |
| `telegram_web_channel_search` | See `sandbase_describe_tool` for details. |
| `telegram_web_channel_info` | See `sandbase_describe_tool` for details. |
| `facebook_bulk_groups` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
