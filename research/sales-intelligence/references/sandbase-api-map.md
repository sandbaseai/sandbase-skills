# SandBase Sales Intelligence API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `apollo_company_enrich` | See `sandbase_describe_tool` for details. |
| `apollo_company_news_search` | See `sandbase_describe_tool` for details. |
| `linkedin_web_v2_company_profile` | See `sandbase_describe_tool` for details. |
| `linkedin_web_v2_user_profile` | See `sandbase_describe_tool` for details. |
| `twitter_web_search_timeline` | See `sandbase_describe_tool` for details. |
| `google_news_bulk_articles` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
