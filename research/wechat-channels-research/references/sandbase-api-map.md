# SandBase WeChat Channels Research API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `wechat_channels_v2_search_channel_videos` | See `sandbase_describe_tool` for details. |
| `wechat_channels_v2_channel_info` | See `sandbase_describe_tool` for details. |
| `wechat_channels_v2_user_videos` | See `sandbase_describe_tool` for details. |
| `wechat_channels_v2_video_detail` | See `sandbase_describe_tool` for details. |
| `wechat_channels_v2_video_comments` | See `sandbase_describe_tool` for details. |
| `wechat_channels_v2_live_detail` | See `sandbase_describe_tool` for details. |
| `wechat_channels_v2_live_history` | See `sandbase_describe_tool` for details. |
| `wechat_channels_v2_user_profile` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
