# SandBase Competitive Pricing API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `strale_pricing_page_extract` | See `sandbase_describe_tool` for details. |
| `google_shopping_bulk_products` | See `sandbase_describe_tool` for details. |
| `amazon_bulk_product_details` | See `sandbase_describe_tool` for details. |
| `context_dev_extract_structured_data` | See `sandbase_describe_tool` for details. |
| `firecrawl_scrape` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
