# SandBase Trend Spotter API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `twitter_web_trending` | See `sandbase_describe_tool` for details. |
| `tiktok_app_v3_home_feed` | See `sandbase_describe_tool` for details. |
| `youtube_web_v2_general_search` | See `sandbase_describe_tool` for details. |
| `google_news_bulk_articles` | See `sandbase_describe_tool` for details. |
| `reddit_app_popular_feed` | See `sandbase_describe_tool` for details. |
| `weibo_web_hot_search` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
