# SandBase China Social Research API Map

Use these tools through the SandBase gateway. Before each call, use `sandbase_describe_tool` to obtain current parameters, then use `sandbase_call_tool` with the exact tool name.

| Tool name | Use it for |
|---|---|
| `weibo_web_search` | See `sandbase_describe_tool` for details. |
| `douyin_search_general_search_v2` | See `sandbase_describe_tool` for details. |
| `xiaohongshu_app_v2_search_notes` | See `sandbase_describe_tool` for details. |
| `bilibili_web_general_search` | See `sandbase_describe_tool` for details. |
| `zhihu_web_hot_list` | See `sandbase_describe_tool` for details. |
| `kuaishou_app_search_comprehensive` | See `sandbase_describe_tool` for details. |

Read-only research only. Do not take actions on external platforms.
